import 'package:flutter/material.dart';

class Forum extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(25),
      child: Text('Forum', style: TextStyle(fontSize:40),),
      color: Colors.blueAccent,
    );
  }
}
